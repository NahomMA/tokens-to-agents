# The repo is managed with uv — prefer `uv sync` from the repo root, which
# installs the shared core package and these deps from the committed lockfile.
#
# This file exists so Part 2 can also be installed standalone with plain pip:
numpy>=1.26
matplotlib>=3.8
